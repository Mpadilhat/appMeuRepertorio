import colors from './colors';
import icons from './icons';

const shadow = {
  shadowColor: colors.dark,
  shadowOffset: {
    width: 0,
    height: 5,
  },
  shadowOpacity: 0.5,
  shadowRadius: 0,
  elevation: 5,
};

const shadowButton = {
  shadowColor: colors.dark,
  shadowOffset: {
    width: 0,
    height: 5,
  },
  shadowOpacity: 0.5,
  shadowRadius: 0,
  elevation: 3,
};

export {colors, shadow, shadowButton, icons};
